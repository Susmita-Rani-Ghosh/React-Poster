import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)

//WHen building react interfaces, we have a root componant 
// that i s APP component here, and h=inside the App component we use the other components 

